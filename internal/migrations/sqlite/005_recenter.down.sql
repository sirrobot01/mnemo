-- Re-center is a one-way destructive reset. Reversing it back into
-- the retired memory/rules product is intentionally not supported.
DROP TABLE IF EXISTS working_states;
DROP TABLE IF EXISTS task_sessions;
DROP TABLE IF EXISTS tasks;
